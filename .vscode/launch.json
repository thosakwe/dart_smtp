{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "example:hello",
            "type": "dart-cli",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "debugSettings": "${command.debugSettings}",
            "program": "${workspaceRoot}/example/hello.dart",
            "args": []
        }
    ]
}